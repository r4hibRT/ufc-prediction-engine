[pytest]
testpaths = tests
pythonpath = .
markers =
    db: needs the PostgreSQL database in .env (skipped when it is unreachable)
filterwarnings =
    ignore:Pandas requires version:UserWarning
    ignore:A NumPy version:UserWarning
    ignore:pandas only supports SQLAlchemy:UserWarning
