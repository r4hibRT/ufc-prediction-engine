# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=
DB_USER=
DB_PASSWORD=

# Model insight paragraphs (optional). The provider and models are set in
# src/engine/voice.py; without a key, narration is skipped.
API_KEY=
